/*Just cast you char at an int*/


import java.util.*;

public class Main {

	public static void main(String[] String) {
	    
		char ch;
		Scanner sc = new Scanner(System.in);
		ch = sc.next().charAt(0);;
	
        int ascii = ch;
        // You can also cast char to int
        int castAscii = (int) ch;
		
		System.out.println("The ASCII value is :"+castAscii);
	}
}
